name: megasprite
description: GPU-accelerated sprite rendering system using spatial binning and fragment shaders
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  shaders:
    - shaders/sprite_shader.frag
