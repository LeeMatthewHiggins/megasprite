name: megasprite
description: GPU-accelerated sprite rendering system using spatial binning and fragment shaders
version: 0.1.0
repository: https://github.com/LeeMatthewHiggins/megasprite

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  archive: ^3.6.1
  flutter:
    sdk: flutter
  synchronized: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^7.0.0

flutter:
  shaders:
    - shaders/sprite_shader.frag
